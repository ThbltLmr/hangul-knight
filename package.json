{
  "name": "hangul-knight",
  "version": "0.1.0",
  "description": "A 2D platformer game for learning Korean Hangul",
  "type": "module",
  "scripts": {
    "dev": "bun build src/main.ts --outdir . --watch",
    "build": "bun build src/main.ts --outdir dist --minify && cp index.html dist/ && cp -r public/* dist/",
    "preview": "cd dist && python3 -m http.server 8080",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
